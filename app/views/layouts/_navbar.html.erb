<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <%= link_to "Bookery", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" data-toggler="collapse" data-target="#navbarCollapse">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav ml-auto">
        <% if logged_in? %>
          <li class="nav-item">
            <%= link_to "hi, #{current_user.username}", user_path(current_user), class: "nav-link" %> 
          </li>
          <li class="nav-item">
            <% if author = Author.find_by(id: params[:author_id]) %>
              <%= link_to "Add Book", new_author_book_path(author), class: "nav-link" %>
            <% else %>
              <%= link_to "Add Book", new_book_path, class: "nav-link" %>
            <% end %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <% if logged_in? %>
            <%= link_to "Sign Out", logout_path, method: :delete, class: "nav-link" %> 
          <% else %>
            <%= link_to "Sign In", login_path, class: "nav-link" %>
          <% end %>  
        </li>
      </ul>
    </div>
  </div>
</nav>